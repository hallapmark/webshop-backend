# For local development, include a dev.env (gitignored) in the root of the project.
# Fill in values for secrets and credentials as needed.

SPRING_PROFILES_ACTIVE=dev
# <- dev,seed when you want to seed the db.

DEV_SPRING_JPA_HIBERNATE_DDL_AUTO=update

DEV_DB_URL=jdbc:postgresql://localhost:5432/webshop
DEV_DB_USER=
DEV_DB_PASSWORD=

# for local dev, you can generate the JWT key with:
# python scripts/generate_jwt_key.py
DEV_JWT_SIGNING_KEY_BASE64=

# CloudFront CDN base URL (e.g., https://d123abc.cloudfront.net)
DEV_CLOUDFRONT_BASE_URL=

